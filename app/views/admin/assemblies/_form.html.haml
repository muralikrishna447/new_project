= semantic_form_for [:admin, @assembly] do |f|
  = f.inputs do
    = f.input :assembly_type, as: :select, collection: Assembly::ASSEMBLY_TYPE_SELECTION
    = f.input :title
    = f.input :description
    = f.input :image_id
    = f.input :youtube_id

  %h2 Assembly Inclusions
  = link_to_add_fields "Add Assembly Inclusion", f, :assembly_inclusions
  = f.semantic_fields_for :assembly_inclusions do |builder|
    = render 'assembly_inclusion_fields', f: builder
  = f.actions

#assembly_selection_data
  #activities_selection_data{data: {:'activities-selection' =>Activity.where("title <> ''").order(:title).select('id, title').to_json}}