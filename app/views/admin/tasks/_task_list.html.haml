%p
  = task_button 'Create display quantities', admin_tasks_create_display_quantities_path
  Copies the decimal value of quantity of ingredients
  into the display_quantity field.

%p
  = task_button 'Create new copy entries', admin_tasks_create_new_copy_path
  Non-destructively creates new copy entries from the copy.yml file.

%p
  = task_button 'Publish all activities', admin_tasks_publish_all_activities_path

%p
  = task_button 'Create activity slugs', admin_tasks_create_activity_slugs_path

%p
  = task_button 'Verify model change assumptions', admin_tasks_verify_model_change_assumptions_path
