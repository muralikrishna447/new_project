.row
  .span12
    %h3 Colors

:ruby
  color_names = %w[black white gray grayMedium grayDark grayDarker grayLight grayLighter white brick
    brickLight brickDark blue blueDark]

- color_names.in_groups_of(11, false).each do |colors|
  .row.show-grid
    - colors.each do |color|
      .span1.styleguide-color{ class: color}= color

