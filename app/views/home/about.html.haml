= content_for :title, 'About'

.container.about-quote
  .row
    .span12
      .lead.section.text-center
        %h1.whitespace Every cook, cooking smarter.

.container.about-mission
  .row
    .span12
      %h2 About ChefSteps
      .row
        .span6
          :markdown
            #{@about_left}
        .span6
          :markdown
            #{@about_right}

= render 'about_tabs'
= render 'about_get_to_work'
= render 'layouts/footer'