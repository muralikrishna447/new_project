.container
  .row
    .span12
      %h1= @poll.title

.container
  .row
    .span12(ng-controller='VotingController')
      - @poll.votables.each do |votable|
        = content_tag_for :div, votable do
          .votable-vote-up
            %a.votable-btn
              %i.icon-chevron-up
          .votable-text
            %h2= votable.title
          .votable-sharing
            %a.votable-btn
              %i.icon-share-alt
          .votable-comments
            %a.votable-btn
              %i.icon-comment-alt
