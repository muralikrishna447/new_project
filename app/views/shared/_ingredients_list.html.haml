.ingredients
  .ingredient-table-body(ui-sortable="sortOptions" ng-model="#{source}.ingredients" ng-init="ingredientSource = '#{source}'")
    .ingredient-table-row.anim-basic-fade.relative(ng-repeat="ai in #{source}.ingredients")
      %csIngredientEditPair


