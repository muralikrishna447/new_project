.matrix(ng-class="'matrix-' + columns + '-per-row'")
  .matrix-item(ng-repeat='item in items' component-item item='item' item-type-name="itemTypeName" theme='theme' char-limit='charLimit' button-message='buttonMessage')
