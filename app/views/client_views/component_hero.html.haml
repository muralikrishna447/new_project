.hero
  .hero-content
    .hero-image
      %img(ng-src='{{content.image}}')
    .hero-cta
      %h2
        {{content.title}}
      %a.btn.btn-primary(ng-href='{{content.url}}')
        {{component.metadata.content.buttonMessage}}
