window.NavigationBootstrap = (target) ->
  $("<link rel=stylesheet type='text/css' href='http://<%= DOMAIN %>/assets/global_navigation.css' />").appendTo('head')
  $.ajax(
    type: "GET"
    url: "http://<%= DOMAIN %>/global-navigation"
  ).done (html) ->
    $(target).html(html)

