#
# Format:
# 
# <stage name>:
#   app: <Heroku app name>
#   stack: <Heroku stack, optional>
#   tag: <git tag pattern, optional>
#   repo: <git repository, optional>
#   config:
#     - <Heroku config:var name>: <Heroku config:var value>
#
production:
  app: delve-production
  stack: cedar
  config:
    BUNDLE_WITHOUT: "development:test:guard"

staging:
  stack: cedar
  app: delve-staging
  config: &default
    BUNDLE_WITHOUT: "development:test:guard"
  addons:
    - memcache:5mb
    - logging:basic
